{
  "name": "@aptiloop/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./development-fixtures": "./src/development-fixtures.ts",
    "./schema": "./src/schema.ts",
    "./seed": "./src/seed.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --sourcemap --dts --out-dir dist",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx src/cli/migrate.ts",
    "db:backup": "tsx ../../scripts/create-approved-backup.ts",
    "db:seed": "tsx src/cli/seed.ts",
    "lint": "eslint src test --max-warnings=0",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aptiloop/course-authoring-kit": "0.1.0",
    "@aptiloop/learning-core": "0.1.0",
    "@aptiloop/shared": "0.1.0",
    "drizzle-orm": "1.0.0-rc.4"
  },
  "devDependencies": {
    "@aptiloop/curriculum": "0.1.0",
    "drizzle-kit": "1.0.0-rc.4",
    "tsup": "^8.5.1"
  }
}
