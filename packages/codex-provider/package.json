{
  "name": "@aptiloop/codex-provider",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --sourcemap --dts --out-dir dist",
    "lint": "eslint src test --max-warnings=0",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aptiloop/agent-core": "0.1.0",
    "@aptiloop/shared": "0.1.0"
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
