PORT=8787
HOST=127.0.0.1
WEB_ORIGIN=http://127.0.0.1:3000
DATABASE_URL=.data/dev-learning-harness.sqlite
DATABASE_BACKUP_DIR=.data/backups
WORKSPACE_ROOT=workspaces/exercises
EXERCISE_ATTEMPTS_ROOT=.data/exercise-attempts
ZED_EXECUTABLE=zed
OPENCODE_ENDPOINT=http://127.0.0.1:4096
OPENCODE_SERVER_USERNAME=opencode
# OPENCODE_SERVER_PASSWORD is read from the environment and never stored.
# Codex reuses the local Codex CLI authentication store.
